
<%= tiny_mce_stuff if can? :update, ContentBlock%>
<% if  can?(:update, ContentBlock) || !@about_stats_text.value.blank? %>
  <div id="about_stats_text" class="row">
    <%= editable_content_block @about_stats_text %>
  </div>
<% end %>
